# Judaan Japan Ambassador Challenge — 동의·서명 시스템 셋업 가이드

QR 스캔 → (모바일) 3단계로 동의서 확인 → 8개 항목 동의 → 손으로 전자서명 → 제출
→ **hjhan@ianglobal.im로 서명 PDF 자동 발송**.
그리고 **상품 계획서 PDF 다운로드 랜딩 페이지**도 함께 포함되어 있습니다.
백엔드/서버/DB 없이 무료로 동작합니다.

---

## 📦 파일 구성

| 파일 | 용도 |
|---|---|
| `sign.html` | 인플루언서가 QR로 접속하는 **모바일 동의·서명 페이지** (한국어+영어) |
| `index.html` | **상품 계획서 다운로드** 랜딩 페이지 |
| `Code.gs` | 이메일 발송용 **Google Apps Script** (무료 서버리스) |
| `qr-generator.html` | 배포 URL을 넣으면 **QR 코드 PNG**를 만들어주는 도구 |
| `product-plan.pdf` | 다운로드용 상품 계획서 (← **실제 파일로 교체**) |
| `vendor/` | 서명·PDF·QR 라이브러리 (동봉, 외부 CDN 불필요) |

### 동의·서명 페이지 특징
- **모바일 전용** 디자인, Judaan **그린** 컬러
- **한국어(진한 글씨) + 영어(회색 글씨)** 병기 → 나중에 일본어가 확정되면 **텍스트만** 바꾸면 됩니다
- **3단계** 진행: ①~④ 동의 → ⑤~⑧ 동의 → 참가자 정보 + 전자서명
- 서명 직전 **8개 동의 항목**을 각각 체크(각 항목 1회 체크), 모두 체크해야 다음/제출 가능
- **손으로 그리는 전자서명**을 증거로 수집 → PDF에 그대로 포함(문자 깨짐 없음)

---

## ✅ 배포 3단계 (약 15분)

### 1단계 — 이메일 발송 엔진 (Google Apps Script, 무료)

이게 "백엔드 대체"입니다. 서버 관리 없이 스크립트만 배포하면 끝납니다.

1. https://script.google.com → **새 프로젝트**
2. 기본 코드를 지우고 `Code.gs` 내용을 **전부 붙여넣기**
3. 상단 `RECIPIENT` 값이 `hjhan@ianglobal.im`인지 확인(변경 가능)
4. 오른쪽 위 **배포 → 새 배포**
   - 유형: **웹 앱** / 실행 계정: **나** / 액세스 권한: **모든 사용자**
   - **배포** → 처음이면 Google 권한 승인
5. 표시되는 **웹 앱 URL** 복사 (`https://script.google.com/macros/s/xxxx/exec`)

> ⚠️ 메일은 **스크립트를 배포한 Google 계정**에서 발송됩니다(Gmail 무료 한도 하루 약 100통).

### 2단계 — 서명 페이지에 URL 연결

`sign.html`을 열어 아래 줄을 찾아 따옴표 안을 1단계 URL로 교체·저장:

```js
const APPS_SCRIPT_URL = "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";
```
↓
```js
const APPS_SCRIPT_URL = "https://script.google.com/macros/s/xxxx/exec";
```

### 3단계 — 웹에 올리기

**가장 빠름(추천) — Netlify Drop** (회원가입·git 불필요)
1. https://app.netlify.com/drop
2. 이 폴더 **전체**(sign.html, index.html, product-plan.pdf, vendor 포함)를 드래그&드롭
3. 몇 초 뒤 `https://랜덤.netlify.app` 생성 → 완료

**GitHub Pages** (무료): 저장소에 파일 업로드 → Settings → Pages → Branch `main`/`root`
**카페24 등**: FTP로 폴더 전체 업로드

> 접속 주소
> - 서명 페이지: `https://주소/sign.html`
> - 다운로드 페이지: `https://주소/` (또는 `/index.html`)

---

## 🔗 QR 코드 만들기

`qr-generator.html`을 브라우저로 열고 **서명 페이지 주소**(`.../sign.html`)를
붙여넣은 뒤 "QR 코드 만들기" → PNG 저장. 인쇄물·DM에 사용하세요.
(원하시면 최종 주소를 알려주시면 제가 QR을 바로 만들어 드립니다.)

---

## 📝 동의서 내용 수정 / 일본어로 교체

`sign.html` 안의 **`SECTIONS` 배열**이 8개 동의 항목의 원본입니다.
각 항목은 `ko`(한국어)와 `en`(영어)로 되어 있어요:

```js
{no:"①", ko:"참가 동의", en:"Participation Consent",
 body:[
   {p:{ko:"본인은 ...", en:"I fully understand ..."}},
   ...
 ]}
```

- **일본어 확정 시**: `en` 값을 일본어로 바꾸거나, `ko`/`en` 텍스트만 교체하면
  화면과 PDF에 자동 반영됩니다. **구조는 손댈 필요 없습니다.**
- 항목을 추가/삭제하면 체크박스 개수도 자동으로 따라갑니다(현재 8개 기준).

일본어·한국어 텍스트는 PDF에 **이미지로 삽입**되므로 폰트/문자 깨짐 걱정이 없습니다.

---

## 📄 상품 계획서 교체

다운로드용 PDF를 **`product-plan.pdf`** 이름으로 이 폴더에 넣으면 됩니다.
다른 이름을 쓰려면 `index.html`의 `href="product-plan.pdf"` 를 수정하세요.

---

## 🔍 테스트

1. 배포한 `sign.html`을 휴대폰으로 열기
2. 3단계 진행: 동의 8개 체크 → 이름·Instagram·이메일 입력 → 서명 → "서명하고 제출"
3. **hjhan@ianglobal.im** 메일함에 PDF 첨부 메일 확인
   (제목: `【참가 동의 완료】Judaan Japan Ambassador Challenge - 이름`)

메일이 안 오면: ① `APPS_SCRIPT_URL` 정확한지 ② 배포 권한이 "모든 사용자"인지
③ 발송 계정 Gmail 스팸/보낸편지함 확인.

---

## 💡 참고

- Web3Forms·EmailJS는 **파일 첨부가 유료**라, 무료로 PDF를 첨부 발송하기 위해
  Google Apps Script를 사용했습니다.
- 제출된 PDF에는 8개 동의 항목의 동의 여부, 참가자 정보, 손서명, 제출 시각이
  모두 포함되어 증빙으로 사용할 수 있습니다.
- 서명 원본을 Google Drive에도 자동 저장하고 싶으면 Apps Script에 코드를
  추가할 수 있습니다(원하시면 도와드릴게요).
